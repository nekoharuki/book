<h1 class="mb-4">発送するためのやり方</h1>
<h2 class="card-title mt-4"><%= @myitem.title %>を住所: <%= @youitem.user.address %>に発送してください</h2>
<br>
<div class="card mb-3">
  <div class="row g-0">
    <div class="col-md-4">
      <img class="img-fluid rounded-start" src="<%= @myitem.image %>" alt="商品画像">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h3 class="card-title"><%= @myitem.title %></h3>
        <p class="card-text">
          <small class="text-muted"><span class="fw-bold">作者: </span><%= @myitem.author %></small>
        </p>
        <p class="card-text">
          <small class="text-muted"><span class="fw-bold">出版社: </span><%= @myitem.publisher %></small>
        </p>
        <p class="card-text">
          <small class="text-muted"><span class="fw-bold">状態: </span><%= @myitem.condition %></small>
        </p>
        <p class="card-text">
          <small class="text-muted"><span class="fw-bold">内容: </span><%= @myitem.content %></small>
        </p>
        <p class="card-text">
          <small class="text-muted"><span class="fw-bold">ジャンル: </span><%= @myitem.category %></small>
        </p>
        <a class="btn btn-primary" href="<%= "/items/#{@hashids.encode(@myitem.id)}/traded" %>"><%= @myitem.title %>の詳細</a>
      </div>
    </div>
  </div>
</div>

  <% if @number == 1 %>
    <%if  @detail.user_offered_status != 1 %>
    <div class="d-flex justify-content-center">
      <%= form_tag("/items/delivery_success/#{@hashids.encode(@number)}/#{@hashids.encode(@myitem.id)}/#{@hashids.encode(@youitem.id)}", method: :post, class: "form-inline") do %>
        <input type="submit" value="発送できた" class="btn btn-success btn-lg">
      <% end %>
    </div>
    <%end%>
  <%end%>

  <% if @number == 2 %>
    <%if  @detail.user_requested_status != 1 %>
    <div class="d-flex justify-content-center">
      <%= form_tag("/items/delivery_success/#{@hashids.encode(@number)}/#{@hashids.encode(@myitem.id)}/#{@hashids.encode(@youitem.id)}", method: :post, class: "form-inline") do %>
        <input type="submit" value="発送できた" class="btn btn-success btn-lg">
      <% end %>
    </div>
    <%end%>
  <%end%>

<br>
<br>
