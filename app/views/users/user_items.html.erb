<div class="d-flex justify-content-between align-items-center">
  <h1><%= @user.name %>様の出品一覧</h1>

  <%= render "users/follow", user: @user %>

<br>
<br>
<% @items.each do |item| %>
  <%= render "items/card", item: item %>

<% end %>